import React from 'react';

export const Loader = ({...props}: {[key: string]: any}) => <span>Loading Icon</span>;
export const RotateCcw = ({...props}: {[key: string]: any}) => <span>Reset Icon</span>;
export const Volume2 = ({...props}: {[key: string]: any}) => <span>Sound On Icon</span>;
export const VolumeX = ({...props}: {[key: string]: any}) => <span>Sound Off Icon</span>;